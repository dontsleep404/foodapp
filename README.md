SplashView
    Connect server
Login view
    Login : {
        User + pass => 
        => Status + name + role
    }
    Login as guest {
        ... =>
        => True + Guest + Guest
    }
MenuView
    Order {
        
    }